<UserControl x:Class="Prueba.view.childViews.Factura"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Mis Facturas Pendientes -->
        <GroupBox Header="Mis Facturas Pendientes" FontWeight="Bold" FontSize="14" Grid.Column="0" Margin="5">
            <DataGrid ItemsSource="{Binding ReparacionesPendientes}"
                      SelectedItem="{Binding ReparacionSeleccionada}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="5"
                      Style="{StaticResource datagridFactura}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Vehículo" Binding="{Binding Vehiculo.Matricula}" Width="100"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente.Nombre}" Width="150"/>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="100"/>
                    <DataGridTextColumn Header="Total Estimado" Binding="{Binding PrecioTotal}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Facturar -->
        <GroupBox Header="Facturar" FontWeight="Bold" FontSize="14" Grid.Column="1" Margin="5">
            <ScrollViewer>
                <StackPanel Margin="10" VerticalAlignment="Top">

                    <!-- Cliente -->
                    <GroupBox Header="Cliente" FontSize="12">
                        <StackPanel Margin="5">
                            <TextBox Text="{Binding Cliente.Nombre}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Cliente.DNI}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Cliente.Telefono}" Margin="0,0,0,5"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Vehículo -->
                    <GroupBox Header="Vehículo" FontSize="12" Margin="0,10,0,0" Height="97">
                        <StackPanel Margin="5">
                            <TextBox Text="{Binding Vehiculo.Matricula}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Vehiculo.Marca}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Vehiculo.Modelo}" Margin="0,0,0,5"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Mecánico -->
                    <GroupBox Header="Mecánico" FontSize="12" Margin="0,10,0,0" Height="53">
                        <StackPanel Margin="5">
                            <TextBox Text="{Binding NombreMecanico}" Margin="0,0,0,5" IsReadOnly="True"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Precio total -->
                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Precio Total:" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text="{Binding PrecioTotal, StringFormat=C}" Margin="10,0,0,0" FontSize="14"/>
                    </StackPanel>

                    <!-- CRUD / Acciones -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Eliminar" Command="{Binding EliminarFacturaCommand}" Margin="0,0,10,0" Width="80" Style="{StaticResource btnEliminarFactura}"/>
                        <Button Content="Facturar" Command="{Binding ConfirmarFacturaCommand}" Width="80" Style="{StaticResource btnFacturar}"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
